language: python

python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
  - "nightly"

sudo: false

matrix:
  allow_failures:
    - python: nightly

install:
  - pip install -r requirements-dev.txt

script:
  - make lint
  - make test
  - make coverage

after_success:
  coveralls
